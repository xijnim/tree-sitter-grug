on_spawn() {
    set_counter_name("Shots counter")
}

on_tick() {
    # comment
    if map_has_i32(me, "shots") {
        shots: i32 = map_get_i32(me, "shots")
        print_string("shots:")
        print_i32(shots)
    } else {
        helper_hi()
    }

    i: i32 = 0

    while i < 10 {
        println()
        i = i + 1
    }
}

helper_hi() string {
    
}
